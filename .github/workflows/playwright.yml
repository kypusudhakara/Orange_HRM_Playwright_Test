name: OrangeHRM employee lifecycle

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - run: npm run typecheck
      - run: npm test
      - name: Save actual HTML report, video and traces
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: orangehrm-playwright-results
          path: |
            playwright-report/
            test-results/
          if-no-files-found: warn
          retention-days: 7
